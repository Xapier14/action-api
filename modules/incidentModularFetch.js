export async function fetchIncidents(
  pageOffset,
  limit,
  location,
  buildingId,
  severityStatus,
  fromInspectorId
) {
  // return promise that returns empty array
  return new Promise((resolve, reject) => {
    resolve([]);
  });
}
